version: "3.9"

services:
  telegram-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: home_chore_bot
    environment:
      - TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
      - DB_URL=sqlite:///home_chore_bot.db
    restart: unless-stopped
